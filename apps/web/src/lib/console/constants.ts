/** Cookie storing active tenant for console UI + API. */
export const CONSOLE_ORG_COOKIE = "aegis_console_org";

/** Fixed dev org id from seed — override via AEGIS_DEV_ORGANIZATION_ID. */
export const DEV_ORGANIZATION_ID = "00000000-0000-4000-8000-000000000010";

export const API_KEY_PREFIX = "att_live_";
