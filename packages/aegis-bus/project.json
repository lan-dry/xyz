{
  "name": "aegis-bus",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/aegis-bus/src",
  "tags": ["type:lib", "scope:aegis"],
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec vitest run",
        "cwd": "packages/aegis-bus"
      }
    }
  }
}
