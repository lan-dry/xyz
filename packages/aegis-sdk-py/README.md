# salanor-aegis-ledger (Python, P0)

Scaffolding for the local Aegis slice. Mirrors `@salanor/aegis-ledger-sdk` validation against `spec/aps/v0.1.json`.

```bash
cd packages/aegis-sdk-py
python -m pip install -e ".[dev]"
pytest
```
